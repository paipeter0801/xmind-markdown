{
  "name": "xmind-markdown-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker — AI help backend for xmind-markdown (Workers AI / qwen)",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:ui": "vitest --ui"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.0",
    "@cloudflare/workers-types": "^4.20240620.0",
    "typescript": "^5.9.3",
    "vitest": "^2.1.0",
    "wrangler": "^3.90.0"
  }
}
